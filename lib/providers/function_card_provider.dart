import 'package:flutter/material.dart';

class FunctionCardProvider {
  String name;
  IconData icon;
  String screenName;
  FunctionCardProvider(
      {required this.icon, required this.name, required this.screenName});
}
